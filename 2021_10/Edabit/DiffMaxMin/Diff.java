package Edabit.DiffMaxMin;

public class Diff {
    public static void run() {
       int[] array = {1, -2, 3, 5, 60};
       /* int max = Integer.MIN_VALUE;
        int min = Integer.MAX_VALUE;

        for (int i : array) {
            if (i > max) {
                max = i;
            } if (i < min) {
                i = min;
            }

            int answer = max - min;
            System.out.print("This is the answer: " + answer);
    } */
/*     int max = array[0];
    for (int i = 0; i < array.length; i++) {
        if (array[i] > max) {
            array[i] = max;
            
        }
        System.out.print(max);
    } 
    int min = array[0];
    for (int i = 0; i < array.length; i++) {
        if (array[i] < min) {
            array[i] = min;
            
        }
        System.out.println(min);
    } */
}
}


